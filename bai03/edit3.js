let a = Number(prompt("Nhập giá trị cạnh hình vuông :"));
if ( a >= 0) {
    let dt = a * a;
    alert("Hình vuông cạnh " + a + " có diện tích là : " + dt);
} else {
    alert("Hãy nhập số > 0 !!!")
}